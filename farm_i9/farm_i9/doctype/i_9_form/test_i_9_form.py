"""Stub tests for I-9 Form. Fleshed out in a later phase once a test site
with ERPNext + HR is available in CI.
"""

import unittest


class TestI9Form(unittest.TestCase):
    def test_placeholder(self):
        # TODO(Phase 2): assert SSN normalization, retention calc, and
        # workflow transitions once a bench test site is wired up.
        self.assertTrue(True)
